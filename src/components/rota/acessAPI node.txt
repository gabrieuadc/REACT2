//Acess_API

const express = require('express');
const app = express();
const router = express.Router();

router.get('/AcessAPIRota', (req, res, next) => {
	res.status(200).send({
		mensagem:'Usando o get dentro da rota 20'
	});
});

function teste(){
	require('http').get('http://localhost:3000/AcessAPIRota', (res) => {
		res.setEncoding('utf8');
		res.on('data', function (body) {
			console.log(body);
		});
	})}

export default Teste


//
//<button onclick = {teste}>Acess API</button>